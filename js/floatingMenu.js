(function($){
	
	$.fn.foatingMenu = function(arg) {
		
		var options = $.extend(
				menu: 'floating-menu',
		);
		
		return this.length ? this
				.each(function(){
					
				}) : this;
		
	};
}(jQuery));